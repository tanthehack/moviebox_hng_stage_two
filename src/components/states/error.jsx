import error from '../../assets/error.svg'
import { useRouteError } from "react-router-dom";

export const ErrorState = () => {
    <section className="w-[100vw] h-[100vh] bg-gray-900">
        hi
        <img src={error} />
    </section>
}